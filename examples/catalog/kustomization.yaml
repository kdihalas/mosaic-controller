apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: catalog-api
  namespace: catalog
spec:
  interval: 10m
  sourceRef:
    kind: ExternalArtifact
    name: catalog-api
  path: ./deploy
  targetNamespace: catalog
  prune: true
  wait: true
  timeout: 5m
