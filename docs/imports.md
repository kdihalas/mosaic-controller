# Imports

External import sources are not supported. Packages, variants, profiles, and policies must be baked into the primary OCI artifact. This keeps one immutable, auditable build input and matches Mosaic's current pre-alpha model.
