# Values

External values are not supported by Mosaic and are not part of this API. There is no inline values field, ConfigMap integration, Secret integration, watch, or related RBAC. Use Mosaic variants baked into the source artifact and select them with `spec.variants`.
